import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RegistroComponent } from './registro.component';
import { AngularFirestore, AngularFirestoreCollection, AngularFirestoreDocument } from 'angularfire2/firestore';


describe('RegistroComponent', () => {
  
  /*it('Debe de crear un nuevo usuario', ()=>{
    var afs: AngularFirestore
    let registro = new RegistroComponent(afs);
    expect(registro.addUsuario('usuario1@mail.to', 'usuario1', 'hola1', '')).toBe(true);  
  });

  /*it('NO debe de crear un nuevo usuario', ()=>{
    var afs: AngularFirestore
    let registro = new RegistroComponent(afs);
    expect(registro.addUsuario('', 'usuario2', 'hola2', 'zona 12 usac')).toBe(false);  
  });*/

});
